#-----------------------------------------------------------------------
# 添加是否复查以前查出的隐患字段
#------------------------------------------------------------------------
ALTER TABLE c_check_task ADD is_review  TINYINT DEFAULT 0 NOT NULL COMMENT '是否复查以前查出的隐患';